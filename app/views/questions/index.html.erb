<h1>Questions</h1>
<nav>
  <ul>
    <li><%= link_to 'Home', root_path %></li>
  </ul>
</nav>

<%= @questions.each do |question| %>
<% end %>



<% @questions.each do |question| %>

<p><%= question.title %> <%= question.body %> <%= question.id %></p>
<p><%= link_to 'Edit', edit_question_path(question.id) %></p>

<!-- делает ссылку на редактирование конкретного вопроса по id -->

<% end %>