<%= render 'questions/errors' , object: @answer %>

<%= form_with model: [@question, @answer] do |f| %>

  <%= f.label :body %>
  <%= f.text_area :body %>
  <%= f.submit "Submit answer!" %>


<% end %>